{
  "$schema": "https://opencode.ai/config.json",
  "command": {
    "dev": {
      "description": "启动前端 dev server + API 后端",
      "prompt": "Start both servers in the background:\n1. `cd /home/Xzz/Code/zheting && npm run dev`\n2. `cd /home/Xzz/Code/api-enhanced && node app.js`\nWait for both to be ready, then report their URLs."
    }
  }
}
