# 代码审计与优化检查表（2026-08-04）

本表只记录已核实的问题。`已修复` 表示代码已修改但仍需完成全部验证；`待后续` 表示风险真实，但不适合与本轮功能修复混在同一提交。

| ID | 严重度 | 范围 | 问题与影响 | 状态 | 验证 |
|---|---|---|---|---|---|
| A-01 | 高 | 登录 | `LoginOverlay` 调用 `tick()` 但未导入，切换手机号/邮箱模式会运行时报错 | 已修复 | 渲染自检 + 交互回归 |
| A-02 | 中 | 登录 | 取消二维码轮询只清理定时器，Promise 永久 pending 并保留闭包 | 已修复 | 源码自检 + 登录弹窗回归 |
| A-03 | 高 | 发布检查 | 版本脚本只识别 LF，Windows CRLF 的 `Cargo.lock` 会让 `check:versions` 永久失败 | 已修复 | `pnpm check:versions` |
| A-04 | 中 | API | 调用方主动 abort 被错误包装成“请求超时”，上层无法区分取消与故障 | 已修复 | `client-timeout.test.js` |
| A-05 | 中 | API/加载 | 每次返回页面都需要重新查询持久缓存，短时间返回仍出现 loading | 已修复 | 内存热缓存 + 页面回归 |
| A-06 | 高 | 移动端 | 所有页面启动时同时挂载，隐藏页面也执行请求与副作用 | 已修复 | 渲染自检 + Network 回归 |
| A-07 | 中 | 移动端 | 所有主标签共用一个滚动位置，切换标签后位置和底栏隐藏状态串页 | 已修复 | 移动端交互回归 |
| A-08 | 中 | 桌面端 | 路由条件渲染外再套 `{#key}`，形成不必要的第二重强制销毁边界 | 已修复 | 渲染自检 + 页面切换回归 |
| A-09 | 中 | 加载体验 | 发现页首屏只有一个转圈和大片空白，没有稳定布局占位 | 已修复 | 桌面/移动截图回归 |
| A-10 | 中 | 无障碍 | 全局移除 outline，但没有为所有交互控件提供统一键盘焦点 | 已修复 | 键盘导航回归 |
| A-11 | 中 | Windows 原生 | `CoInitializeEx` 成功后线程退出前没有配对 `CoUninitialize` | 已修复 | Rust check/test |
| A-12 | 中 | 登录 | 切回扫码模式时事件函数与响应式 effect 各启动一次二维码请求 | 已修复 | 渲染自检 + 登录弹窗回归 |
| A-13 | 中 | CSS 架构 | 大量硬编码 `z-index`、圆角和末尾 `!important` 覆盖，浮层与视觉细节容易回归 | 待后续 | 分层 token 专项重构 |
| A-14 | 低 | UI | 桌面未登录态内容密度偏低，播放器与主内容关系较弱 | 待后续 | 登录/未登录双状态设计回归 |

## 验证门槛

- 所有 Node 自检通过。
- 版本一致性检查通过。
- Vite 生产构建通过且无新增警告。
- Rust 测试或 `cargo check` 通过。
- 桌面 1440px、桌面窄窗口、移动端 390px 三种视口完成截图检查。
- 登录弹窗模式切换、主标签切换、返回页面、滚动恢复完成手动交互检查。
## 本轮验证结果

- 版本一致性检查：通过，版本 `1.3.0`。
- Node 自检：14/14 文件通过。
- Vite 生产构建：通过，283 个模块，无 Svelte 警告。
- Windows 目标文件 rustfmt：通过。
- 桌面与 390px 移动端截图：通过，移动端骨架屏与底部布局正常。
- Rust 全量测试：环境阻塞。依赖已下载，但本机 MinGW `gcc/ld` 在基础 crate build script 阶段返回 53。
- 浏览器自动点击：环境阻塞。浏览器控制连接被 Windows 沙箱刷新错误终止；登录事件链由构建和源码自检覆盖。